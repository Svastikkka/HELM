apiVersion: v1
kind: Service
metadata:
  name: {{ include "celery-redis.fullname" . }}
  labels:
    {{- include "celery-redis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "celery-redis.selectorLabels" . | nindent 4 }}
