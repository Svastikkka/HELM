apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "celery-worker.fullname" . }}-test-connection"
  labels:
    {{- include "celery-worker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "celery-worker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
